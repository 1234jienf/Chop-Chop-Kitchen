# GPT-4o mini 손님 심사평 설정

API 키는 브라우저 코드에 넣지 않고 프로젝트 루트의 `.env`에서 Python 서버만 읽습니다.

`.env` 파일:

```dotenv
OPENAI_API_KEY=sk-본인의-API-키
```

`.env`는 `.gitignore`에 등록되어 Git에 포함되지 않습니다. 운영 환경에 이미 설정된 환경 변수가 있으면 그 값을 우선 사용합니다.

PowerShell에서 서버를 실행하는 예시:

```powershell
python py/review_server.py
```

리뷰 전용 서버는 Whisper, TensorFlow, PyTorch를 불러오지 않습니다. `py/api.py`는 별도의 Whisper 음성 변환 기능까지 필요한 경우에만 사용합니다.

게임은 `http://127.0.0.1:8000/review`로 결산 데이터를 보냅니다. 서버는 GPT-4o mini로 다음 JSON을 생성합니다.

- 4개 음식별 한국어 한 줄 심사평
- 하루 전체 한국어 한 줄 최종 평가

API 키가 없거나 요청이 실패하면 브라우저의 로컬 심사평을 사용하므로 다음 날 진행은 막히지 않습니다.

## 결산 규칙

- 음식 점수: 해당 음식에 포함된 공정 점수의 평균
- 코스 평균: 4개 음식 점수의 평균
- 손님 니즈: 관련 공정이 모두 70점 이상이면 충족
- 니즈 보너스: 최종 점수 +5점, 팁 +100G
- 음식별 매출: `75G + 음식 점수 × 2G`
- 하루 매출: 네 음식 매출 합계 + 니즈 보너스 팁

불맛 손님의 니즈는 메인 코스의 굽기 공정에서만 판정합니다. 손질과 끓이기 손님의 니즈는 네 코스 전체의 해당 공정에서 판정합니다.
