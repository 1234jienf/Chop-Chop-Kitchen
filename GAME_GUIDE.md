# Chop-Chop Kitchen 게임 소개 및 설명

## 게임 제목 및 한 줄 소개

### Chop-Chop Kitchen

> 직접 학습한 음성 인식 AI를 활용해 세 명의 셰프가 목소리로 요리 공정을 이어 가는 3인 협동 브라우저 요리 게임

`Chop-Chop Kitchen`은 플레이어가 마이크에 조리 소리를 내며 4코스 요리를 완성하는 협동 게임입니다. 세 명의 플레이어가 한 명씩 돌아가며 자르기, 끓이기, 굽기, 섞기, 뿌리기·붓기 등의 공정을 수행하고, 각 공정의 정확도에 따라 코스 점수와 매출이 결정됩니다.

조리 음성은 프로젝트에서 직접 학습한 Teachable Machine Audio 모델로 분류합니다. 브라우저에서 TensorFlow.js와 Speech Commands를 이용해 `Tak`, `Chap`, `Ssuk`, `Ssak`, `Huu`, `Ting` 등의 소리를 인식하며, 오븐 정지와 일부 대본 입력에는 Web Speech API를 함께 사용합니다. 하루가 끝나면 OpenAI의 GPT-4o mini가 플레이 결과를 바탕으로 손님의 미식 리뷰를 생성합니다. AI 리뷰를 사용할 수 없을 때는 게임에 내장된 리뷰가 대신 표시됩니다.

## 게임 방법

### 목표

세 명의 플레이어가 정해진 차례에 맞춰 에피타이저, 스타터, 메인, 디저트로 구성된 4코스의 모든 조리 공정을 완료하는 것이 목표입니다.

각 공정을 정확하게 수행해 높은 코스 점수와 별점을 얻고 매출을 올려야 합니다. 손님이 선호하는 종류의 공정을 모두 70점 이상으로 완료하면 최종 점수 5점과 매출 100G를 추가로 받을 수 있습니다.

### 게임 시작

1. 한 PC에서 웹 서버와 멀티플레이 서버를 실행합니다.
2. 세 명의 플레이어가 브라우저에서 게임 페이지를 엽니다.
3. 모두 같은 방 코드를 입력하고 `방 참가`를 누릅니다.
4. 플레이어 카드를 드래그해 조리 순서를 정합니다.
5. 세 명 모두 `준비하고 시작하기`를 누르면 게임이 시작됩니다.

게임 중에는 현재 차례인 플레이어만 조리 입력을 사용할 수 있습니다. 나머지 두 플레이어는 같은 조리 화면과 진행 상태를 실시간으로 확인합니다.

### 조작 방법

조리 화면에서 `마이크 켜기`를 누르고 브라우저의 마이크 권한을 허용합니다.

| 조리 공정 | 조작 방법 |
| --- | --- |
| 자르기 | 화면에 표시되는 리듬에 맞춰 `탁`, `찹`, `사각`, `스윽` 등의 소리를 냅니다. |
| 끓이기 | `Huu(후우)` 소리를 내어 끓이기 진행도를 채웁니다. |
| 일반 굽기 | `Huu(후우)` 소리로 화력을 올리고, 입을 다물어 화력을 낮추면서 목표 화력 구간을 유지합니다. |
| 오븐 굽기 | 타이머와 목표 지점을 확인한 뒤 알맞은 순간에 `띵` 소리를 내어 오븐을 멈춥니다. `딩`, `ding`도 인식합니다. |
| 섞기 | 화면에 제시된 대본을 읽습니다. 목소리의 음량과 인식 결과가 움직임과 점수에 반영됩니다. |
| 뿌리기·붓기 | 화면에 표시된 목표 음정을 내고 정해진 시간 동안 유지합니다. |

마이크 입력을 사용할 수 없는 환경에서는 화면의 성공 입력, 실수 입력 및 테스트용 버튼으로 진행할 수 있습니다.

### 점수와 보상

- 각 공정은 20~100점으로 평가됩니다.
- 코스 점수는 해당 코스에 포함된 공정 점수의 평균입니다.
- 최종 점수는 네 코스 점수의 평균에 손님 취향 보너스를 더해 계산하며 최대 100점입니다.
- 별점은 최종 점수 20점당 1개로 계산하며 최소 1개, 최대 5개입니다.
- 각 코스의 매출은 `75 + 코스 점수 × 2`입니다.
- 손님 취향 조건을 달성하면 최종 점수 `+5`, 하루 매출 `+100G`를 받습니다.
- 누적 자금이 3,000G 이상이 되면 식당 등급이 1성에서 2성으로 올라갑니다.

GPT-4o mini는 계산이 끝난 결과를 자연스러운 미식 리뷰로 표현하는 역할만 하며 게임의 점수와 매출에는 영향을 주지 않습니다.

### 종료 조건

한 게임의 플레이 범위는 4코스의 모든 공정을 완료하고 손님 리뷰와 정산 결과를 확인할 때까지입니다.

정산 화면에서 `정산하고 다음 날`을 누르면 획득한 매출이 누적되고 새로운 손님과 함께 다음 날을 시작합니다. 현재 버전에는 별도의 최종 엔딩 조건이 없으므로 플레이어가 정산을 마치고 게임을 종료하거나 다음 날 플레이를 계속할 수 있습니다.

## 실행 방법

### 권장 환경

- Python 3.10 이상
- 최신 Chrome 또는 Chromium 기반 브라우저
- 마이크가 연결된 PC 또는 모바일 장치
- 멀티플레이 시 같은 네트워크에 연결된 브라우저 3개

### 1. 저장소 설치

```powershell
git clone https://github.com/1234jienf/Chop-Chop-Kitchen.git
cd Chop-Chop-Kitchen
```

### 2. 서버 의존성 설치

```powershell
python -m pip install fastapi "uvicorn[standard]"
```

### 3. 웹 서버 실행

프로젝트 루트에서 다음 명령을 실행합니다.

```powershell
python -m http.server 8080
```

실행 후 서버 PC에서는 <http://localhost:8080>에 접속합니다.

### 4. 멀티플레이 + GPT 리뷰 서버 실행

별도의 PowerShell 창을 프로젝트 루트에서 열고 다음 명령을 실행합니다.

```powershell
python -m uvicorn py.multiplayer_api:app --host 0.0.0.0 --port 8001
```

다른 플레이어는 서버 PC와 같은 네트워크에서 아래 주소로 접속합니다.

```text
http://서버-PC의-IP-주소:8080
```

서버 PC의 방화벽 설정에 따라 TCP 8080번과 8001번 포트의 인바운드 연결을 허용해야 할 수 있습니다.

### 5. GPT-4o mini 손님 리뷰 설정 (선택)

`.env.example`을 복사해 `.env` 파일을 만들고 OpenAI API 키를 입력합니다.

```powershell
Copy-Item .env.example .env
```

```dotenv
OPENAI_API_KEY=your-api-key
```

4단계에서 실행한 서버가 GPT 리뷰도 함께 처리하므로 별도 서버를 실행할 필요가 없습니다. API 키가 없거나 API 요청에 실패하면 내장된 손님 리뷰가 자동으로 표시됩니다.

## 플레이 링크 또는 설치 방법

- 저장소: <https://github.com/1234jienf/Chop-Chop-Kitchen>
- 로컬 플레이 주소: <http://localhost:8080>
- 다른 PC의 접속 주소: `http://서버-PC의-IP-주소:8080`
- 공개 웹 플레이 URL: 현재 제공되지 않음
- APK 및 별도 설치 파일: 현재 제공되지 않음

현재 버전은 별도의 설치 파일 없이 저장소를 내려받아 Python HTTP 서버로 실행하는 웹 게임입니다. ES Module과 브라우저 마이크 API를 사용하므로 `index.html` 파일을 직접 열지 말고 위 실행 방법에 따라 HTTP 주소로 접속해야 합니다.

## 플레이 영상 링크

현재 저장소에는 공개된 플레이 영상 링크가 등록되어 있지 않습니다.

```text
플레이 영상: 추후 추가 예정
```

영상이 업로드되면 위 항목을 YouTube, Google Drive 또는 제출 플랫폼의 공개 링크로 교체하면 됩니다.
